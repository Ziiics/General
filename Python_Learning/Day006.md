# Functions and Karel

### Functions

- Python has a built-in functions already such as `print()` or `len()`
- For Python, indentation have to be 4 spaces, that's why we uses `tab`

```
# Make your own functions
def my_function():
    # Do this
    # Then do this
    # Finally do this
    # Must be indented

# Calling the function
my_function()
```

### While Loop

- Compared to the for loop, it is best to use when conditions are dynamic

```
# while not_goal() is not reached yet
while at_goal() == False:
    jump()
# or
while not at_goal():
    jump()
```

### Karols

```

```
