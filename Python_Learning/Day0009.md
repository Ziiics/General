# Day 9 - Dictionaries, Nesting, and the Secret Auction

### Dictionaries

```
# Dictionaries example
dictionary_example = {
    "Key1": "Value1",
    "Key2": "Value2",
}

# To take the piece of information,
print(programming_dictionary["Key1"]) # very important to get the right key

# Add/change another key-value
dictionary_example["Key3"] = "Value3"

# To loop through dictionary
for key in dictionary_example:
    print(dictionary_example[key])
```

#### Student grades

```
student_scores = {
    'Harry': 88,
    'Ron': 78,
    'Hermione': 95,
    'Draco': 75,
    'Neville': 60
}

student_grades = { }

for student in student_scores:
    score = student_scores[student]
    if score >= 91:
        student_grades[student] = "Outstanding"
    elif score >= 81 and score <= 90:
        student_grades[student] = "Exceeds Expectations"
    elif score >= 71 and score <= 80:
        student_grades[student] = "Acceptable"
    elif score <= 70:
        student_grades[student] = "Fail"
```

### Nesting

- This can even go crazy where dictionaries inside dictionaries inside list

```
# Nesting List inside List - basically list inside dictionary
nested_list = ["A", "B", ["C", "D"]]
print(nested_list[2][1]) # will print D

# Nesting dictionary inside a Dictionary
my_dictionary = {
    key1: Value,
    key2: {Key: Value, Key: Value},
}
print()
```

#### Secret Auction

```
import os

# TODO-1: Ask the user for input
other = True
bid_list = { }

while other:
    name = input("What is your name? ")
    bid = input("What is your bid? $")

# TODO-2: Save data into dictionary {name: price}
    bid_list[name] = int(bid)

# TODO-3: Whether if new bids need to be added
    other = input("Are there any other bidders? Type 'yes' or 'no'\n")
    if other == "no":
        other = False
    elif other == "yes":
        print("\n" * 20)


# TODO-4: Compare bids in dictionary
highest = 0
for amount in bid_list:
    if highest < bid_list[amount]:
        highest = bid_list[amount]

print(highest)
```
